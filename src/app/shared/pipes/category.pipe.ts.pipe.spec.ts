import { CategoryPipeTsPipe } from './category.pipe.ts.pipe';

describe('CategoryPipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryPipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});
